#ifndef __BSP_MATH_H
#define __BSP_MATH_H

float map(float value, float fromLow, float fromHigh, float toLow, float toHigh);
float normalize(float value, float min, float max);
unsigned int pow(unsigned int x, unsigned char y);

#endif
